# the_todo_app

A very simple todo app ~~that doesn't work yet, because state is not being managed right~~ that works now, because state is managed via attributes and callbacks.

Can be improved by using a state management library like `provider`.